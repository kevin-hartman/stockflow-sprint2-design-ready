# S3 – UI: expose batch and serial as distinct fields

**As a** warehouse operator,
**I want** batch_number and serial_number displayed as separate, labeled fields wherever the combined inventory_code was shown,
**so that** I can read and act on each tracking dimension individually.
