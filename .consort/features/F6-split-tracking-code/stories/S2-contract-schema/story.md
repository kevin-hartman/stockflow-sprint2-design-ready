# S2 – Contract schema: drop inventory_code column

**As a** warehouse operator,
**I want** the combined inventory_code column physically removed from the stock table,
**so that** the schema is fully refactored with no legacy combined code remaining.
