# The backend the Vite dev server proxies /api and /health to. run-dev.sh sets
# this automatically to the backend it boots; set it by hand only when you run
# `npm run dev` in this directory directly.
VITE_PROXY_TARGET=http://localhost:8000
